        public void CaminhoPastaExisteOuCria(string pasta) {

            var caminho = "C:\PASTAB" + pasta;

            if (!Directory.Exists(caminho)) {                
                Directory.CreateDirectory(caminho);
            }
               
        }